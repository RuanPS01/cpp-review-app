#include <iostream>
#include <iomanip>


using namespace std;

int main (){
   

    double maior, menor, altura;
    
    cin >> N;
    cin >> maior;
    cin >> menor;
    cin >> altura;
    
        while (N > 0)
        {
            if (altura < maior)
            maior = maior;  
           
         else if (altura < maior)
            {
                altura = menor ;
                
            }
            
        }
        
    
    cout << "Menor altura: " << menor << endl;
    cout << "Maior altura: " << maior << endl;
    
    return (0);
}