#include <iostream> 
using namespace std;

int main ()
{
    int N;
    int horarios [100]; // quantidade (100) de valores possíveis de tempo a serem inseridos;  
    double Ttreino; // os valores 
    cin >> horarios [100];
    
    int soma = 0;
    int media;
    
    for (int i = 0 ; i > N ; i++)
    { 
        cin >> Ttreino;
        if (Ttreino >0 && <0)
        {
            soma == ( Ttreino += soma / 2 );
        }
        
    }
    
    cout << "Media do tempo de treino: " << media ;
    
    return 0; 
}

// ler o tempo de treino dos alunos
// mostrar o maior tempo
//fazer a média dos tempos informados