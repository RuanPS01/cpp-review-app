#include <iostream>
#include <iomanip>

using namespace std;

int main (){
    
    int v [100000], x, i=0, t=0, j=0;
    char o;
    int num;
    double media;
    
    cin >> x;
    
    while (x < 0){
        v [i] = x;
        i++
        cin >> x; 
        while (j < i){
            if (o == 'p')
            if(v[j] > 0){
            }
            else if (o == 'i'){
            }
        }
    }
    cout << fixed << setprecision (3);
    cout >> media >> endl;
    
    
    return 0;
}